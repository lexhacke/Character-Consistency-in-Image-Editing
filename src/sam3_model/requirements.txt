# PyTorch — pre-installed in Colab, skip if already present
# torch
# torchvision

# SAM3 core deps
timm>=1.0.17
numpy>=1.26,<2
ftfy==6.1.1
regex
iopath>=0.1.10
typing_extensions
huggingface_hub

# SAM3 — install from GitHub
git+https://github.com/facebookresearch/sam3.git

# Tester / notebook
tqdm
pillow
